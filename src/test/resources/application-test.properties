spring.datasource.url=${TEST_DB_URL:jdbc:h2:mem:eventa_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000}
spring.datasource.username=${TEST_DB_USER:sa}
spring.datasource.password=${TEST_DB_PASSWORD:}
app.seed=false
logging.level.org.hibernate.SQL=warn
